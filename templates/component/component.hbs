import React from 'react'; import { cn } from '@/utils/cn'; export interface
{{pascalCase name}}Props { className?: string; children?: React.ReactNode; } export const
{{pascalCase name}}
= ({ className, children }:
{{pascalCase name}}Props) => { return (
<div className="{cn(''," className)}>
  {children}
</div>
); };