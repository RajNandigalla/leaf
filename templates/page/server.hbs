import type { GetServerSideProps, InferGetServerSidePropsType } from 'next';
import Head from 'next/head';

export const getServerSideProps = (async (context) => {
  // Fetch data from external API or database
  return {
    props: {
      // Add your props here
    },
  };
}) satisfies GetServerSideProps<{
  // Define your props type here
}>;

export default function {{pascalCase name}}(
  props: InferGetServerSidePropsType<typeof getServerSideProps>
) {
  return (
    <>
      <Head>
        <title>{{pascalCase name}} | LeafInk</title>
        <meta name="description" content="{{pascalCase name}} page" />
      </Head>

      <div className="min-h-screen p-8">
        <h1 className="text-4xl font-bold mb-8">{{pascalCase name}}</h1>
        {/* Add your page content here */}
      </div>
    </>
  );
}
